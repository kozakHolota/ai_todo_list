[project]
name = "ai-todo-list"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "langgraph",
    "langchain",
    "langchain-core",
    "langchain-openai",
    "pydantic",
    "ipykernel>=7.2.0",
    "jupyter>=1.1.0",
]
